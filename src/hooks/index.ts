// Export all hooks
import { useColor } from "./useColor";
import { useCopyToClipboard } from "./useCopyToClipboard";
import { useSwatches } from "./useSwatches";

export { useColor, useCopyToClipboard, useSwatches };
